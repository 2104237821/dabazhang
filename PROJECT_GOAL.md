# 打八张 Web 游戏：项目总目标

> 目标版本：1.0  
> 状态：已确认，未经用户确认不得改变

## 北极星目标

制作一款可部署到私人服务器、可由四名真人或真人与机器人混合游玩的“打八张”Web 游戏。服务端必须是唯一规则权威，客户端只显示玩家有权看到的信息。

## 首版成功标准

- 游客通过昵称和六位房间码创建、加入、准备并开始游戏。
- 房主可添加、移除或一键补满机器人；四个座位满员且所有真人准备后才能开始。
- 完整实现 `docs/GAME_RULES.md` 中的规则 v1.0。
- 支持刷新恢复、断线 60 秒宽限与机器人接管、在线行动 45 秒单步托管。
- 支持一局结束后的结算和再来一局。
- 桌面端使用原创四方 2D 牌桌，本人固定在下方、队友在上方。
- 普通 Node.js 与 Docker 均可部署；提供健康检查和反向代理说明。
- 私有 GitHub 仓库保存任务级提交、CI 结果和版本标签，可使用 `git revert` 回滚。

## 硬约束

- Node.js 24 LTS、TypeScript、npm workspaces。
- React + Vite 客户端；Fastify + Socket.IO 服务端。
- 规则引擎不得依赖 UI、网络、数据库或系统时间。
- 客户端不得收到其他玩家手牌或牌堆顺序。
- 首版只支持单 Node 进程、单服务器实例和内存房间。
- 最低桌面尺寸 1100×650；不开发手机竖屏或触摸专用界面。
- 所有图形、头像、牌背和装饰必须原创；参考图仅用于四方围桌的信息布局。

## 首版非目标

账号、数据库、跨重启房间恢复、战绩、排行榜、聊天、观战、商城、支付、手机端、跨服务器房间和多实例扩容均不在首版范围内。

## 变更规则

`PROJECT_GOAL.md` 与 `docs/GAME_RULES.md` 是项目方向的唯一来源。子代理发现缺口时必须停止相关任务并登记到 `docs/DECISIONS.md`，不得自行改变规则。只有主协调代理在用户确认后可以修改这两个文件。
